# AIVO Backend - Python FastAPI

## 环境配置

| 环境变量 | 说明 | 示例 |
|---------|------|------|
| `DB_PASSWORD` | MySQL 数据库密码 | `your_password` |
| `REDIS_PASSWORD` | Redis 密码（可选） | `your_redis_password` |
| `JWT_SECRET` | JWT 密钥 | `your_jwt_secret_key_min_32_chars` |
| `XUNFEI_APP_ID` | 讯飞应用 ID | `your_app_id` |
| `XUNFEI_API_KEY` | 讯飞 API Key | `your_api_key` |
| `XUNFEI_API_SECRET` | 讯飞 API Secret | `your_api_secret` |
| `SILICONFLOW_API_KEY` | SiliconFlow API Key | `your_siliconflow_key` |
| `DEEPSEEK_API_KEY` | DeepSeek API Key | `your_deepseek_key` |

## 启动方式

```bash
# 激活 conda 环境
conda activate meet

# 安装依赖
pip install -r requirements.txt

# 启动服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8765
```

## API 文档

启动后访问: http://localhost:8765/docs
